import { Component } from '@angular/core';

@Component({
  selector: 'app-home',
  standalone: true,
  templateUrl: './home.component.html',
  styleUrls: ['./home.component.css'] // Corrección aquí
})
export class HomeComponent {
  // Lógica del componente (si la necesitas)
}
